<%= form_for(@trip) do |f| %>
  <% if @trip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@trip.errors.count, "error") %> prohibited this trip from being saved:</h2>

      <ul>
      <% @trip.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title, autofocus: "on", :class => "form-control" , :placeholder => "My Trip Title"%>
  </div>
  <div class="field">
    <%= f.label :about %><br>
    <%= f.text_area :about, :class => "form-control" , :placeholder => "About Your Trip!" %>
  </div>
  <div class="field">
    <%= f.label :start_Date %><br>
    <%= f.date_select :start_date, :order => [:day, :month, :year] %>
  </div>
  <div class="field">
    <%= f.label :end_Date %><br>
    <%= f.date_select :end_date, :order => [:day, :month, :year] %>
  </div>
  <div class="actions">
    <%= f.submit "Submit", :class => "btn my-btn" %>
  </div>
<% end %>
